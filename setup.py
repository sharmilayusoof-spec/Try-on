"""
Setup script for VTO Backend
"""
from setuptools import setup, find_packages

setup(
    name="vto-backend",
    version="1.0.0",
    packages=find_packages(),
    python_requires=">=3.9",
)
